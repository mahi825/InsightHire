import { useState } from 'react'
import { Button, Card, LinkButton, PageHeader, Progress } from '../components/ui'
import { activities, dashboardStats, jobs, scoreHistory } from '../data/mockData'
import { applyToJob, saveProfile, saveSettings, searchJobs, submitResume } from '../services/api'

export function DashboardPage() { return <><PageHeader eyebrow="YOUR DASHBOARD" title="A clearer path to your next role." description="Track progress, strengthen your profile, and keep your job search moving." action={<LinkButton to="/upload">Upload resume</LinkButton>} /><div className="stat-grid">{dashboardStats.map(stat => <Card key={stat.label} className="stat-card"><span>{stat.label}</span><strong>{stat.value}</strong><small>{stat.detail}</small></Card>)}</div><div className="dashboard-grid"><Card><div className="card-heading"><div><h2>Profile completion</h2><p>Complete your profile to improve job matches.</p></div><strong className="score">78%</strong></div><Progress label="Profile details" value={78} /><div className="quick-actions"><LinkButton to="/profile" variant="secondary">Complete profile</LinkButton><LinkButton to="/analysis" variant="secondary">View analysis</LinkButton></div></Card><Card><div className="card-heading"><div><h2>Weekly progress</h2><p>Your applications this week</p></div><strong className="score">4</strong></div><div className="bar-chart">{[34, 58, 48, 80, 63, 44, 70].map((height, i) => <span key={i} style={{ height: `${height}%` }} />)}</div><div className="chart-labels"><span>M</span><span>T</span><span>W</span><span>T</span><span>F</span><span>S</span><span>S</span></div></Card></div><Card><div className="card-heading"><div><h2>Recent activity</h2><p>Updates from your career workspace</p></div><button className="text-button">View all</button></div><div className="activity-list">{activities.map(([title, time, tag]) => <div key={title}><span className="activity-dot" /><div><strong>{title}</strong><small>{time}</small></div><em>{tag}</em></div>)}</div></Card></> }

export function ResumePage() { const [status, setStatus] = useState(''); async function upload(e) { e.preventDefault(); await submitResume(Object.fromEntries(new FormData(e.currentTarget))); setStatus('Resume received. Parsing will begin after backend integration.') } return <><PageHeader eyebrow="RESUME" title="Your resume, ready to work harder." description="Upload a current resume to receive parsing, ATS, and skill-match insights." /><div className="two-column"><Card><h2>Upload resume</h2><p className="muted">PDF or DOCX, up to 10 MB.</p><form onSubmit={upload}><label className="dropzone"><input type="file" name="resume" accept=".pdf,.doc,.docx" required /><b>Choose a file</b><span>or drag and drop it here</span></label><Button type="submit">Upload and parse</Button>{status && <p className="success-message">{status}</p>}</form></Card><Card><h2>Current resume</h2><div className="resume-preview"><div className="paper-lines"><i /><i /><i /><i /><i /></div></div><div className="split-row"><span>resume_arjun_kumar.pdf</span><button className="text-button">Replace</button></div></Card></div><Card><div className="card-heading"><div><h2>Parsed information</h2><p>Preview of the data the backend will return after parsing.</p></div><span className="status-badge">Ready to parse</span></div><div className="info-grid"><Info title="Skills" text="React, JavaScript, CSS, Git" /><Info title="Experience" text="2 years · Frontend development" /><Info title="Education" text="B.Tech, Computer Science" /><Info title="Certifications" text="Add certifications after parsing" /></div></Card></> }

export function JobsPage() { const [message, setMessage] = useState(''); async function find(e) { e.preventDefault(); await searchJobs(Object.fromEntries(new FormData(e.currentTarget))); setMessage('Search is ready to connect to the job-matching API.') } return <><PageHeader eyebrow="JOB MATCHING" title="Roles that fit your profile." description="Search recommendations and compare your skills with each opportunity." /><Card><form className="job-search" onSubmit={find}><label className="field"><span>Search jobs</span><input name="query" placeholder="Job title, skill, or company" /></label><label className="field"><span>Work type</span><select name="workType"><option value="">All types</option><option>Full-time</option><option>Hybrid</option><option>Remote</option></select></label><label className="field"><span>Location</span><input name="location" placeholder="Any location" /></label><Button type="submit">Search</Button></form>{message && <p className="success-message">{message}</p>}</Card><div className="job-list">{jobs.map(job => <Card className="job-card" key={job.id}><div className="job-card-top"><div><span className="company-mark">{job.company[0]}</span><div><h2>{job.title}</h2><p>{job.company} · {job.location}</p></div></div><strong className="match-score">{job.match}% match</strong></div><div className="skill-tags">{job.skills.map(skill => <span key={skill}>{skill}</span>)}<span>{job.type}</span></div><div className="job-actions"><Button variant="secondary" onClick={() => setMessage(`${job.title} saved to your shortlist.`)}>Save job</Button><Button onClick={() => applyToJob({ jobId: job.id }).then(() => setMessage(`Application started for ${job.title}.`))}>Apply now</Button></div></Card>)}</div></> }

export function AnalysisPage() { return <><PageHeader eyebrow="RESUME ANALYSIS" title="Understand what your resume communicates." description="Use these insights to focus revisions before your next application." action={<LinkButton to="/resume" variant="secondary">Update resume</LinkButton>} /><div className="analysis-grid"><Card className="ats-card"><span>ATS score</span><strong>82<small>/100</small></strong><Progress label="Strong match" value={82} /></Card><Card><h2>Skill match</h2><p className="muted">Your strongest matching skills</p><div className="skill-match"><span>React <b>94%</b></span><span>JavaScript <b>89%</b></span><span>CSS <b>86%</b></span></div></Card><Card><h2>Resume strength</h2><p className="muted">Clear impact statements and a focused technical profile.</p><span className="status-badge">Strong foundation</span></Card></div><div className="two-column"><Card><h2>Missing keywords</h2><p className="muted">Consider adding relevant terms where they accurately reflect your experience.</p><div className="skill-tags"><span>TypeScript</span><span>Testing</span><span>Performance</span><span>Agile</span></div></Card><Card><h2>Suggestions</h2><ul className="clean-list"><li>Add measurable outcomes to your latest role.</li><li>Move key frontend skills closer to the top.</li><li>Tailor your summary for target positions.</li></ul></Card></div></> }

export function InterviewPage() { const [started, setStarted] = useState(false); return <><PageHeader eyebrow="INTERVIEW PREPARATION" title="Practice with purpose." description="Choose a format, answer focused questions, and review your performance." /><div className="interview-grid">{[['Technical interview', 'Role-specific questions on frontend fundamentals.'], ['HR interview', 'Practice communication and career storytelling.'], ['Coding questions', 'Work through common problem-solving prompts.']].map(([title, desc]) => <Card key={title}><span className="interview-icon">◌</span><h2>{title}</h2><p>{desc}</p><Button variant="secondary" onClick={() => setStarted(true)}>Start practice</Button></Card>)}</div>{started && <Card className="session-card"><div><p className="eyebrow">MOCK INTERVIEW</p><h2>Tell us about a frontend project you are proud of.</h2><p>Future AI interview API will provide questions, feedback, and a performance report here.</p></div><Button onClick={() => setStarted(false)}>End session</Button></Card>}<Card><h2>Performance report</h2><div className="report-grid"><Info title="Last score" text="78 / 100" /><Info title="Strongest area" text="Technical clarity" /><Info title="Focus next" text="Concise examples" /></div></Card></> }

export function AnalyticsPage() { return <><PageHeader eyebrow="ANALYTICS" title="See your search in motion." description="A simple view of applications, interviews, and profile growth over time." /><div className="stat-grid">{[['Applications', '12'], ['Interviews', '3'], ['Average ATS score', '76%'], ['Skill growth', '+18%']].map(([label, value]) => <Card className="stat-card" key={label}><span>{label}</span><strong>{value}</strong><small>Last 30 days</small></Card>)}</div><div className="two-column"><Card><div className="card-heading"><div><h2>Resume score history</h2><p>Five most recent analyses</p></div><span className="status-badge">Improving</span></div><div className="line-chart">{scoreHistory.map((value, i) => <span key={value} style={{ height: `${value}%`, left: `${i * 23}%` }}><i /></span>)}</div><div className="chart-labels"><span>Mar</span><span>Apr</span><span>May</span><span>Jun</span><span>Jul</span></div></Card><Card><h2>Application stages</h2><div className="stage-list"><Progress label="Applied" value={100} /><Progress label="Screening" value={58} /><Progress label="Interview" value={25} /><Progress label="Offer" value={8} /></div></Card></div></> }

export function ProfilePage() { const [message, setMessage] = useState(''); async function save(e) { e.preventDefault(); await saveProfile(Object.fromEntries(new FormData(e.currentTarget))); setMessage('Profile changes are ready to send to the backend.') } return <><PageHeader eyebrow="PROFILE" title="Keep your profile current." description="A complete profile improves recommendations and helps you apply faster." /><Card><form className="profile-form" onSubmit={save}><h2>Personal information</h2><div className="form-grid"><Field label="Full name" name="fullName" defaultValue="Arjun Kumar" /><Field label="Email address" name="email" type="email" defaultValue="arjun@example.com" /><Field label="Phone number" name="phone" defaultValue="+91 98765 43210" /><Field label="Location" name="location" defaultValue="Bengaluru, India" /></div><label className="field"><span>Professional summary</span><textarea name="summary" defaultValue="Frontend developer focused on responsive, accessible web experiences." /></label><h2>Skills</h2><label className="field"><span>Skills</span><input name="skills" defaultValue="React, JavaScript, CSS, Git" /></label><Button type="submit">Save changes</Button>{message && <p className="success-message">{message}</p>}</form></Card></> }

export function SettingsPage() { const [message, setMessage] = useState(''); async function save(e) { e.preventDefault(); await saveSettings(Object.fromEntries(new FormData(e.currentTarget))); setMessage('Settings are ready to sync when the backend is connected.') } return <><PageHeader eyebrow="SETTINGS" title="Control your workspace." description="Manage preferences, privacy, and account options." /><Card><form className="settings-form" onSubmit={save}><section><h2>Notifications</h2><label className="toggle"><input name="jobAlerts" type="checkbox" defaultChecked /><span>Job recommendations and alerts</span></label><label className="toggle"><input name="interviewReminders" type="checkbox" defaultChecked /><span>Interview reminders</span></label></section><section><h2>Privacy</h2><label className="toggle"><input name="profileVisibility" type="checkbox" /><span>Make profile visible to recruiters</span></label></section><section><h2>Appearance</h2><label className="field"><span>Theme</span><select name="theme" defaultValue="light"><option value="light">Light</option><option value="system">System default</option></select></label></section><Button type="submit">Save settings</Button>{message && <p className="success-message">{message}</p>}</form></Card><Card className="danger-card"><h2>Delete account</h2><p>Deleting your account will be handled securely by the backend.</p><Button variant="danger">Delete account</Button></Card></> }

function Field({ label, name, type = 'text', ...rest }) { return <label className="field"><span>{label}</span><input name={name} type={type} {...rest} /></label> }
function Info({ title, text }) { return <div className="info-item"><span>{title}</span><strong>{text}</strong></div> }
